# Chemistry Agent API 环境变量配置
# 复制此文件为 .env 并填入你的API密钥

# ===== 必需的API密钥 =====
# DeepSeek API密钥（必需）
DEEPSEEK_API_KEY=sk-bd9bde5fe1864c1cb23dccfd6759815c

# OpenAI API密钥（可选，用于备用）
OPENAI_API_KEY=your_openai_api_key_here

# ===== 可选的API密钥 =====
# SERP API密钥（用于网络搜索）
SERP_API_KEY=your_serp_api_key_here

# RXN4Chem API密钥（用于化学反应预测）
RXN4CHEM_API_KEY=your_rxn4chem_api_key_here

# ChemSpace API密钥（用于化学空间查询）
CHEMSPACE_API_KEY=your_chemspace_api_key_here

# Semantic Scholar API密钥（用于学术文献搜索）
SEMANTIC_SCHOLAR_API_KEY=your_semantic_scholar_api_key_here

# ===== 服务器配置 =====
# 服务器主机地址（默认：0.0.0.0）
HOST=0.0.0.0

# 服务器端口（默认：8000）
PORT=8000

# ===== 模型配置 =====
# 默认LLM模型
DEFAULT_MODEL=deepseek-chat

# 默认工具选择模型
DEFAULT_TOOLS_MODEL=deepseek-chat

# DeepSeek API基础URL
DEEPSEEK_BASE_URL=https://api.deepseek.com

# OpenAI API基础URL（备用）
OPENAI_BASE_URL=https://api.openai.com/v1

# 默认温度参数
DEFAULT_TEMPERATURE=0.1

# 默认最大迭代次数
DEFAULT_MAX_ITERATIONS=40

# ===== Embedding配置 =====
# Embedding模型名称（推荐模型见下方注释）
EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2

# Embedding模型缓存目录
EMBEDDING_CACHE_DIR=./models/embeddings

# 计算设备（auto, cpu, cuda, cuda:0, cuda:1等）
EMBEDDING_DEVICE=auto

# 是否使用CUDA加速
EMBEDDING_USE_CUDA=true

# 批处理大小
EMBEDDING_BATCH_SIZE=32

# 是否启用embedding缓存
EMBEDDING_ENABLE_CACHE=true

# ===== WebSearch Reranker配置 =====
# 是否在WebSearch中启用重排序
WEBSEARCH_USE_RERANKER=true

# 是否使用本地重排序器（需要安装sentence-transformers）
WEBSEARCH_USE_LOCAL_RERANKER=false

# 最大返回结果数量
WEBSEARCH_MAX_RESULTS=5

# 重排序后的top-k结果数量
WEBSEARCH_RERANK_TOP_K=5

# 是否启用维基百科搜索
WEBSEARCH_ENABLE_WIKIPEDIA=true

# 是否启用网络搜索
WEBSEARCH_ENABLE_WEB_SEARCH=true

# 搜索超时时间（秒）
WEBSEARCH_TIMEOUT=30

# 结果内容最大长度
WEBSEARCH_MAX_CONTENT_LENGTH=500

# 最小相关性分数阈值
WEBSEARCH_MIN_RELEVANCE_SCORE=0.1

# 化学关键词加权分数
WEBSEARCH_CHEMISTRY_BONUS=0.1

# 标题权重（用于相关性计算）
WEBSEARCH_TITLE_WEIGHT=0.7

# 内容权重（用于相关性计算）
WEBSEARCH_CONTENT_WEIGHT=0.3

# ===== 其他配置 =====
# 是否启用调试模式
DEBUG=False

# 是否启用热重载（开发模式）
RELOAD=True

# 日志级别
LOG_LEVEL=info

# ===== Embedding模型推荐 =====
# 轻量级英文模型（快速）：sentence-transformers/all-MiniLM-L6-v2
# 高质量英文模型：sentence-transformers/all-mpnet-base-v2
# 多语言轻量级模型：sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# 中文优化模型：BAAI/bge-base-zh-v1.5
# 高质量中文模型：BAAI/bge-large-zh-v1.5

# ===== WebSearch Reranker模型推荐 =====
# 推荐的重排序模型：
# - BAAI/bge-reranker-v2-m3 (多语言，高质量)
# - BAAI/bge-reranker-base (英文，轻量级)
# - cross-encoder/ms-marco-MiniLM-L-6-v2 (英文，快速)